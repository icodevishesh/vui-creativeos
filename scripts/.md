your task: Approvals: assign approved copies manually to the respective designers and editors (flow change: automatic → manual). Dont interrupt anything else. Please Carefully change the flow to manual. And provide all the files where you have done changes and details.

current flow: admin/account_manager assigns a task -> automatically assigned to copy_writer/content_writer of selected client team -> creates calendar and copies -> submits -> sent for internal approval -> approved by admin/account_manager -> sent to client for approval -> approved by client -> copies marked as approved -> checked by copies media_type: if media_type == video/reel -> automatically new task created and assigned to video editor of that team; if media_type == image/carousel/text -> automatically new task created and assigned to graphics_designer.

New flow:
admin/account_manager creates a new task and can assign to anyone manually -> assigned to selected member (can be copy_writer/content_writer or anyone) -> creates calendar and copies -> submits -> sent for internal approval -> approved by admin/account_manager -> sent to client for approval -> approved by client -> copies marked as approved -> Now we are not assigning any task to anyone automatically after approval. 
when a copy approved -> it will show all the approved copies in a table list in approvals page -> admin/account_manager can select any copy and assign it to anyone manually -> the approved copy have a button assign to -> after click -> modal opens -> admin/account_manager name the task, assigned to (dropdown with all members), priority, due date, description(optional), publish date & time will be taken as it is from the copy, submit button -> new task created and assigned to selected member -> appear in task board and appear in designer's workspace -> same flow followed.

New component:
- table list of approved copies in Approvals page with dropdown filter according to media_type (video/reel/image/carousel/text)
- admin/account_manager can filter by media_type and if media_type == video/reel then show video_editor else show graphics_designer in assigned to dropdown
- use MantineReactTable to create table.

